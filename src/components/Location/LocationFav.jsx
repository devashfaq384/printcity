import React from 'react'

const LocationFav = ({name}) => {
  return (
        <button className='text-white bg-gray-700 rounded-full h-10 shadow-xl ' >{name}</button>
  )
}

export default LocationFav